/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['d20t6we9nb6rnn.cloudfront.net', 'images.zola.com', 'i.scdn.co'],
  },
}

module.exports = nextConfig
